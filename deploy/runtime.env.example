# Copy this template outside the repository and set mode 0600. Never commit the
# completed file. Preserve the existing keyring and secrets during migration.
APP_URL=https://filedrop.mikaela79.com
DATABASE_URL=
SHARE_TOKEN_KEYRING=
SESSION_SECRET=
# Compose treats $ specially. Put the complete scrypt value inside single
# quotes, for example: UPLOAD_PASSWORD_HASH='$scrypt$...'
UPLOAD_PASSWORD_HASH=
CRON_SECRET=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
