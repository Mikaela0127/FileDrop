# Application
APP_URL=http://localhost:3000
SESSION_SECRET=replace-with-at-least-32-random-characters
UPLOAD_PASSWORD_HASH=replace-with-a-password-hash
CRON_SECRET=replace-with-at-least-32-random-characters

# PostgreSQL (introduced on Day 2)
DATABASE_URL=postgresql://filedrop:filedrop@localhost:5432/filedrop

# Cloudflare R2 (introduced during the upload milestone)
R2_ACCOUNT_ID=replace-me
R2_ACCESS_KEY_ID=replace-me
R2_SECRET_ACCESS_KEY=replace-me
R2_BUCKET_NAME=filedrop-development
